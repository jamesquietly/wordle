# Wordle

PWA to showcase wordle, a game where you have to guess a 5 letter word in 6 tries. Uses SvelteKit.

```
pnpm install
pnpm run dev
```

To deploy, run

```
pnpm run deploy
```

Go to your repository's settings > Pages > Source and select the branch `gh-pages`
